// import { fromJS } from 'immutable';
// import { makeSelectTabsChooserDomain } from '../selectors';

// const selector = makeSelectTabsChooserDomain();

describe('makeSelectTabsChooserDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
