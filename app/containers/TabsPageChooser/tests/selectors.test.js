// import { fromJS } from 'immutable';
// import { makeSelectTabsPageChooserDomain } from '../selectors';

// const selector = makeSelectTabsPageChooserDomain();

describe('makeSelectTabsPageChooserDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
