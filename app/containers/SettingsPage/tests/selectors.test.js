// import { fromJS } from 'immutable';
// import { makeSelectSettingsPageDomain } from '../selectors';

// const selector = makeSelectSettingsPageDomain();

describe('makeSelectSettingsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
